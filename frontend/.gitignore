# Local environments
.env
.env.local

# Auto-generated by scripts/deploy.cjs when chainId === 31337 (Hardhat local).
# Changes on every `hardhat node` restart — never commit it.
public/local-addresses.json

# Node modules
node_modules/

# Hardhat files
artifacts/
cache/
typechain-types/